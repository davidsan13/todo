

const Task = (title, date) => {
  let done = false;

  const getTitle = () => title;
  const getDate = () => date;
  isDone() {
    done = true;
  }
  update
  delete
  return {getTitle, isDone, done}
}